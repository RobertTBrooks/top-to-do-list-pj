# Node modules (npm packages)
node_modules/

# Build output
dist/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store           # macOS
Thumbs.db           # Windows

# dotenv environment files (if used)
.env
.env.local
.env.*.local

# Optional lockfiles (if you're only using one)
package-lock.json
yarn.lock
pnpm-lock.yaml

# VS Code settings (optional)
.vscode/

# Webpack cache (if enabled)
.cache/
webpack.cache/

# IDE metadata (JetBrains, Eclipse, etc.)
.idea/
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln

# Test coverage reports
coverage/

# Mac trash
.Trashes

# Linux
*~

# Optional: browser cache
*.local

# Optional: skip uploaded assets/images (if not managed in git)
public/assets/

